pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        maven {
            url "http://maven.google.com/"
            allowInsecureProtocol = true
        }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            url "http://maven.google.com/"
            allowInsecureProtocol = true
        }
    }
}
rootProject.name = "Neighbours"
include ':app'
include ':data'
include ':domain'
